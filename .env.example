# Cérbero — waitlist service URL (exposed via Nike gateway).
# In production this is the default; override for local dev pointing to a
# local Cérbero instance:
# VITE_CERBERO_URL=http://localhost:8085
VITE_CERBERO_URL=https://api.olympkusai.com/cerbero
